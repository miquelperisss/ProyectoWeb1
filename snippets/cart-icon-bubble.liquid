<a
  href="{{ routes.cart_url }}"
  class="nav-cart"
  data-fore-funny-cart-link
  aria-label="View cart, {{ cart.item_count }} {% if cart.item_count == 1 %}item{% else %}items{% endif %}"
>
  <span aria-hidden="true">🛒</span>
  <span>Cart</span>
  <span>(<span data-cart-count>{{ cart.item_count }}</span>)</span>
</a>
